{
  "name": "crimegpt-monorepo",
  "version": "1.0.0",
  "description": "Orchestrates build and deployment of the CrimeGPT monorepo",
  "main": "backend/server.js",
  "type": "module",
  "scripts": {
    "install:backend": "npm install --prefix backend --legacy-peer-deps",
    "install:frontend": "npm install --prefix frontend --legacy-peer-deps",
    "install:all": "npm run install:backend && npm run install:frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run install:all && npm run build:frontend",
    "start": "node backend/server.js"
  },
  "keywords": [
    "crimegpt",
    "monorepo",
    "deployment"
  ],
  "author": "Antigravity Agent",
  "license": "ISC"
}
